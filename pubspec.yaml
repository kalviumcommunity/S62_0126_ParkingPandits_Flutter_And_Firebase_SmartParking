name: smart_parking_assistant
description: A smart parking discovery app
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # UI Components
  google_fonts: ^6.1.0

  # Navigation & State Management
  go_router: ^13.0.1
  riverpod: ^2.4.9

  # Firebase (for later)
  firebase_core: ^2.24.2
  firebase_auth: ^4.14.1
  cloud_firestore: ^4.15.1
  google_sign_in: ^6.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
